import React from 'react';
import { Reports } from '../../components/reports/Reports';

/** Manager reports page — API-driven energy table (org + venue filters). */
export function ReportsPage() {
  return <Reports />;
}
